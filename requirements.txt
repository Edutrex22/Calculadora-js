# Este projeto é uma aplicação web pura (HTML/CSS/JavaScript)
# Não são necessárias dependências externas para execução

# Para desenvolvimento, você pode querer instalar:
# - Um servidor HTTP local (opcional, já que você pode abrir o index.html diretamente no navegador)
# - Ferramentas de desenvolvimento web (opcional)

# Exemplo de como instalar um servidor HTTP local simples:
# python -m http.server 8000

# Versão do Python (apenas para referência)
python_version >= 3.6
